<rs:RSWindow
    x:Class="RS.HMI.Client.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:areas="clr-namespace:RS.HMI.Client.Views.Areas"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hmi="clr-namespace:RS.HMI.Client.Controls"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RS.HMI.Client.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:rs="https://github.com/luo-ross/Widgets"
    x:Name="this"
    Title="上位机客户端"
    MinWidth="500"
    MinHeight="500"
    d:DataContext="{d:DesignInstance Type=local:HomeViewModel}"
    BorderCornerRadius="5"
    CaptionHeight="36"
    Icon="/Assets/login-bg.jpg"
    IsFitSystem="True"
    IsMaxsizedFullScreen="{Binding ElementName=CkIsFullScreen, Path=IsChecked}"
    ResizeMode="CanResizeWithGrip"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <!--<rs:RSWindow.DataContext>
        <local:HomeViewModel />
    </rs:RSWindow.DataContext>-->
    <rs:RSWindow.Resources>
        <ResourceDictionary>
            <rs:ForegroundColorConverter x:Key="ForegroundColorConverter" />
            <rs:ClipConverter x:Key="ClipConverter" />
            <rs:IconKeyConverter x:Key="IconKeyConverter" />
            <rs:EnumConverter x:Key="EnumConverter" />
            <SolidColorBrush x:Key="RSApp.SolidColorBrush.EBEBEB">#EBEBEB</SolidColorBrush>
            <DataTemplate DataType="{x:Type areas:UserViewModel}">
                <areas:UserView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type areas:RoleViewModel}">
                <areas:RoleView />
            </DataTemplate>
        </ResourceDictionary>
    </rs:RSWindow.Resources>
    <rs:RSWindow.LeftCaptionContent>
        <StackPanel Margin="5,0,5,0" Orientation="Horizontal">
            <Button
                Padding="8"
                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.BrowseBack}"
                rs:ControlsHelper.IconHeight="12"
                rs:ControlsHelper.IconWidth="12"
                Command="{x:Static NavigationCommands.BrowseBack}"
                CommandTarget="{Binding ElementName=PART_Navigate}"
                Style="{StaticResource RSApp.Styles.IconButtonStyle}"
                ToolTip="点击可后退，按住可查看历史记录" />
            <Button
                Padding="8"
                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.BrowseForward}"
                rs:ControlsHelper.IconHeight="12"
                rs:ControlsHelper.IconWidth="12"
                Command="{x:Static NavigationCommands.BrowseForward}"
                CommandTarget="{Binding ElementName=PART_Navigate}"
                Style="{StaticResource RSApp.Styles.IconButtonStyle}"
                ToolTip="点击可前进，按住可查看历史记录" />

            <ToggleButton
                x:Name="PART_BtnNavSwitch"
                Grid.Row="0"
                Margin="3,0,0,0"
                Padding="8"
                HorizontalAlignment="Left"
                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.More}"
                rs:ControlsHelper.IconHeight="12"
                rs:ControlsHelper.IconWidth="12"
                IsChecked="{Binding ElementName=PART_Navigate, Path=IsNavExpanded, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                IsThreeState="False">
                <ToggleButton.Style>
                    <Style BasedOn="{StaticResource RSApp.Styles.IconButtonStyle}" TargetType="{x:Type ToggleButton}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=PART_Navigate, Path=ScreenSize}" Value="Small">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>


        </StackPanel>
    </rs:RSWindow.LeftCaptionContent>
    <rs:RSWindow.MidCaptionContent>
        <Grid Margin="5,0,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Setting}" Style="{StaticResource RSApp.Styles.IconButtonStyle}" />
            </StackPanel>
        </Grid>
    </rs:RSWindow.MidCaptionContent>

    <Grid>
        <rs:RSNavigate x:Name="PART_Navigate" Content="{Binding ViewModelSelect}" />
    </Grid>


    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border
            x:Name="PART_NavHost"
            Grid.Column="0"
            Width="260"
            Background="#FFFFFF">
            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="5"
                    Direction="0"
                    Opacity="0.12"
                    ShadowDepth="1"
                    Color="#A1ACB6" />
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="10,50,0,10">
    -->
    <!--<StackPanel Grid.Row="0" Orientation="Horizontal">
                         <Image
                             Width="30"
                             Height="30"
                             HorizontalAlignment="Left"
                             VerticalAlignment="Center"
                             Source="/Assets/logo.ico" />
                         <TextBlock
                             Margin="5,0,0,0"
                             FontSize="18"
                             Foreground="#475569"
                             Text="HMI-Client" />
                     </StackPanel>-->
    <!--


                    <StackPanel
                        x:Name="PART_CaptionTitle"
                        Grid.Column="0"
                        Orientation="Horizontal">
                        <Border
                            x:Name="PART_Icon"
                            Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=IconWidth, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            Height="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=IconHeight, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            Margin="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=IconMargin, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            VerticalAlignment="Center"
                            CornerRadius="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=IconCornerRadius, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="MouseLeftButtonDown">
                                    <i:InvokeCommandAction Command="{x:Static SystemCommands.ShowSystemMenuCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                            <Border.Background>
                                <ImageBrush ImageSource="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=Icon, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                            </Border.Background>
                        </Border>
                        <TextBlock
                            x:Name="PART_Title"
                            VerticalAlignment="Center"
                            Foreground="{Binding ElementName=PART_NavHost, Path=Background, Converter={StaticResource ForegroundColorConverter}}"
                            Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type rs:RSWindow}}, Path=Title, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <ToggleButton
                        x:Name="PART_TogBtnNav"
                        Margin="0,0,-15,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{DynamicResource RSApp.Styles.NavSideToggleStyle}"
                        WindowChrome.IsHitTestVisibleInChrome="True" />
                </Grid>
                <ScrollViewer Grid.Row="1">
    -->
    <!--<ItemsControl ItemsSource="{Binding NavigateModelList}">
                         <ItemsControl.ItemTemplate>
                             <DataTemplate>
                                 <RadioButton
                                     Margin="0,5,0,5"
                                     rs:ControlsHelper.IconData="{Binding IconKey, Converter={StaticResource IconKeyConverter}}"
                                     Command="{Binding NavCommand}"
                                     Content="{Binding Name}"
                                     GroupName="NavigateGroup"
                                     Style="{StaticResource RSApp.Styles.NavItemStyle}" />
                             </DataTemplate>
                         </ItemsControl.ItemTemplate>
                     </ItemsControl>-->
    <!--


                    <RadioButton
                        Margin="0,5,0,5"
                        rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.More}"
                        Content="测试"
                        GroupName="NavigateGroup"
                        Style="{StaticResource RSApp.Styles.NavItemStyle}" />
                </ScrollViewer>

            </Grid>
        </Border>

        <ScrollViewer Grid.Column="1" Panel.ZIndex="-1">
            <Grid x:Name="PART_ContentHost" Margin="25,15,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Border
                    x:Name="PART_Header"
                    Padding="3,2,3,2"
                    Background="#fff"
                    CornerRadius="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button
                                x:Name="PART_BtnNavShow"
                                Padding="10"
                                HorizontalAlignment="Left"
                                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.More}"
                                rs:ControlsHelper.IconHeight="12"
                                rs:ControlsHelper.IconWidth="15"
                                Style="{StaticResource RSApp.Styles.IconButtonStyle}"
                                Visibility="Collapsed" />


                            <RadioButton Content="控制面板" Style="{StaticResource RSApp.Styles.BreadCrumbStyle}" />
                            <TextBlock Foreground="#7F475569" Text="/" />
                            <RadioButton Content="首页" Style="{StaticResource RSApp.Styles.BreadCrumbStyle}" />
                        </StackPanel>


                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Setting}" Style="{StaticResource RSApp.Styles.IconButtonStyle}" />
                        </StackPanel>

                    </Grid>
                </Border>

                <ContentControl x:Name="PART_Body" />

                <Grid x:Name="PART_Footer" />
            </Grid>
        </ScrollViewer>
    </Grid>-->






    <!--<Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <Button
                        Padding="10"
                        HorizontalAlignment="Left"
                        rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.More}"
                        rs:ControlsHelper.IconHeight="12"
                        rs:ControlsHelper.IconWidth="15"
                        Style="{StaticResource RSApp.Styles.IconButtonStyle}" />
                    <Border Grid.Row="0" Padding="10">
                        <rs:RSSearch
                            x:Name="BtnSearch"
                            Margin="3,0,0,0"
                            IsTextChangedSearch="True"
                            OnBtnSearchCallBack="BtnSearch_OnBtnSearchCallBack"
                            SearchContent="{Binding SearchContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Border>
                </StackPanel>

                <Border Grid.Row="1">

                    <ItemsControl ItemsSource="{Binding NavigateModelList}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton
                                    Margin="0,5,0,5"
                                    rs:ControlsHelper.IconData="{Binding IconKey, Converter={StaticResource IconKeyConverter}}"
                                    Command="{Binding NavCommand}"
                                    Content="{Binding Name}"
                                    GroupName="NavigateGroup"
                                    Style="{StaticResource RSApp.Styles.NavItemStyle}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

    -->
    <!--<ScrollViewer>
                        <StackPanel>
                            <RadioButton
                                Margin="0,5,0,5"
                                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Home}"
                                Command="{Binding NavCommand}"
                                Content="监控中心"
                                Style="{StaticResource RSApp.Styles.NavItemStyle}" />

                            <RadioButton
                                Margin="0,3,0,3"
                                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.SerialPort}"
                                Command="{Binding NavCommand}"
                                Content="串口通讯1"
                                Style="{StaticResource RSApp.Styles.NavItemStyle}" />

                            <RadioButton
                                Margin="0,3,0,3"
                                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.ModbusTCP}"
                                Content="ModbusTCP"
                                Style="{StaticResource RSApp.Styles.NavItemStyle}" />

                            <RadioButton
                                Margin="0,3,0,3"
                                rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.ModbusRTU}"
                                Content="ModbusRTU"
                                Style="{StaticResource RSApp.Styles.NavItemStyle}" />

                            <TreeView
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                ItemsSource="{Binding TreeModelList}">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <Border x:Name="PART_Border">
                                            <Grid Margin="0,5,0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="auto" />
                                                    <ColumnDefinition Width="auto" />
                                                    <ColumnDefinition />
                                                </Grid.ColumnDefinitions>
                                                <Image
                                                    Grid.Column="1"
                                                    Width="15"
                                                    Height="15"
                                                    Margin="5,0,0,0"
                                                    Source="{Binding TreeIcon}" />
                                                <TextBlock
                                                    Grid.Column="2"
                                                    Margin="10,0,0,0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"
                                                    FontSize="12"
                                                    Text="{Binding TreeName}" />
                                            </Grid>
                                        </Border>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </StackPanel>
                    </ScrollViewer>-->
    <!--
                </Border>
                <Border
                    Grid.Row="2"
                    Padding="0,8,0,8"
                    BorderBrush="{StaticResource RSApp.SolidColorBrush.EBEBEB}">
                    <RadioButton
                        Margin="0,5,0,5"
                        rs:ControlsHelper.IconData="{StaticResource RSApp.Icons.Setting}"
                        Command="{Binding NavCommand}"
                        CommandParameter="{Binding}"
                        Content="设置"
                        GroupName="NavigateGroup"
                        Style="{StaticResource RSApp.Styles.NavItemStyle}" />
                </Border>
            </Grid>
        </Border>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Border
                x:Name="PART_ContentHost"
                Margin="10,10,10,0"
                Background="#fff"
                CornerRadius="10">
                <Border.Clip>
                    <MultiBinding Converter="{StaticResource ClipConverter}">
                        <Binding ElementName="PART_ContentHost" Path="CornerRadius" />
                        <Binding ElementName="PART_ContentHost" Path="ActualWidth" />
                        <Binding ElementName="PART_ContentHost" Path="ActualHeight" />
                    </MultiBinding>
                </Border.Clip>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Frame
                        x:Name="PART_Frame"
                        Grid.Row="1"
                        Content="{Binding ViewModelSelect}">
                        <Frame.ContextMenu>
                            <ContextMenu Placement="MousePoint">
                                <MenuItem Header="test1">
                                    <MenuItem.Icon>
                                        <Path
                                            Width="12"
                                            Height="12"
                                            VerticalAlignment="Center"
                                            Data="{StaticResource RSApp.Icons.Asterisk}"
                                            Fill="Red"
                                            Stretch="Fill" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="test2" IsChecked="True" />
                                <MenuItem Header="test3">
                                    <MenuItem Header="test4" />
                                    <MenuItem Header="test5" />
                                    <MenuItem Header="test6" IsChecked="True">
                                        <MenuItem.Icon>
                                            <Path
                                                Width="12"
                                                Height="12"
                                                VerticalAlignment="Center"
                                                Data="{StaticResource RSApp.Icons.Asterisk}"
                                                Fill="Red"
                                                Stretch="Fill" />
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="test7" IsChecked="True">
                                        <MenuItem Header="test8" />
                                        <MenuItem Header="test9" />
                                        <MenuItem Header="test10" IsChecked="True">
                                            <MenuItem.Icon>
                                                <Path
                                                    Width="12"
                                                    Height="12"
                                                    Data="{StaticResource RSApp.Icons.Asterisk}"
                                                    Fill="Red"
                                                    Stretch="Fill" />
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </MenuItem>
                                </MenuItem>
                            </ContextMenu>
                        </Frame.ContextMenu>
                    </Frame>
                </Grid>
            </Border>
            <Border Grid.Row="1" Padding="10">
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="版本：1.0.0.0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>-->
</rs:RSWindow>
