<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
<<<<<<< Updated upstream:RS.Widgets/Themes/RSWinMessageBox.xaml
    xmlns:interface="clr-namespace:RS.Widgets.Interface"
=======
    xmlns:converters="clr-namespace:RS.Widgets.Converters"
    xmlns:interface="clr-namespace:RS.Widgets.Interfaces"
>>>>>>> Stashed changes:RS.Widgets/Themes/RSWinMessage.xaml
    xmlns:rs="clr-namespace:RS.Widgets.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Button.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Effects.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Styles/Controls.Icons.xaml" />
        <ResourceDictionary Source="pack://application:,,,/RS.Widgets;component/Themes/RSWindow.xaml" />
    </ResourceDictionary.MergedDictionaries>
<<<<<<< Updated upstream:RS.Widgets/Themes/RSWinMessageBox.xaml

    <Style BasedOn="{StaticResource RSApp.Styles.RSWindowBaseStyle}" TargetType="{x:Type rs:RSWinMessageBox}">
=======
    <converters:ClipConverter x:Key="ClipConverter" />
    <Style BasedOn="{StaticResource RSApp.Styles.RSWindowBaseStyle}" TargetType="{x:Type rs:RSWinMessage}">
>>>>>>> Stashed changes:RS.Widgets/Themes/RSWinMessage.xaml
        <Setter Property="MaxHeight" Value="450" />
        <Setter Property="MaxWidth" Value="600" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type rs:RSWinMessageBox}">
                    <Border Effect="{StaticResource RSApp.Styles.DefaultDropShadowEffect}">
                        <Border
                            x:Name="PART_Border"
                            Margin="15"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
<<<<<<< Updated upstream:RS.Widgets/Themes/RSWinMessageBox.xaml
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Clip="{TemplateBinding BorderClipRect}">
=======
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.Clip>
                                <MultiBinding Converter="{StaticResource ClipConverter}">
                                    <Binding Path="CornerRadius" RelativeSource="{RelativeSource Mode=TemplatedParent}" />
                                    <Binding ElementName="PART_Border" Path="ActualWidth" />
                                    <Binding ElementName="PART_Border" Path="ActualHeight" />
                                    <Binding ElementName="PART_Border" Path="BorderThickness" />
                                </MultiBinding>
                            </Border.Clip>
>>>>>>> Stashed changes:RS.Widgets/Themes/RSWinMessage.xaml
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto" />
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="auto" />
                                </Grid.RowDefinitions>
                                <Border
                                    Grid.Row="0"
                                    Padding="5"
                                    BorderBrush="#eeeeee"
                                    BorderThickness="0,0,0,1">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Text="{TemplateBinding rs:MessageBoxAttached.Caption}" />
                                </Border>
                                <Grid Grid.Row="1" Background="#fff">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>

                                    <Path
                                        x:Name="PART_Icon"
                                        Width="25"
                                        Height="25"
                                        Margin="25,0,15,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Fill="{TemplateBinding Foreground}"
                                        Stretch="Fill" />

                                    <TextBox
                                        Grid.Column="1"
                                        Margin="0"
                                        Padding="3"
                                        VerticalAlignment="Stretch"
                                        rs:ControlsHelper.CornerRadius="0"
                                        BorderThickness="0"
                                        IsReadOnly="True"
<<<<<<< Updated upstream:RS.Widgets/Themes/RSWinMessageBox.xaml
                                        Text="{TemplateBinding rs:MessageBoxAttached.MessageBoxText}"
                                        TextWrapping="WrapWithOverflow" />
=======
                                        Text="{TemplateBinding MessageBoxText}"
                                        TextWrapping="WrapWithOverflow"
                                        VerticalScrollBarVisibility="Auto" />
>>>>>>> Stashed changes:RS.Widgets/Themes/RSWinMessage.xaml
                                </Grid>
                                <Border
                                    Grid.Row="2"
                                    Padding="5"
                                    Background="#eee"
                                    CornerRadius="0,0,5,5">
                                    <Grid>
                                        <StackPanel
                                            Margin="50,0,0,0"
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal">
                                            <Button
                                                x:Name="PART_BtnYes"
                                                Content="Yes"
                                                Style="{StaticResource RSApp.Styles.PrimaryButton}"
                                                Visibility="Collapsed" />
                                            <Button
                                                x:Name="PART_BtnOK"
                                                Content="OK"
                                                Style="{StaticResource RSApp.Styles.PrimaryButton}"
                                                Visibility="Collapsed" />
                                            <Button
                                                x:Name="PART_BtnNo"
                                                Content="No"
                                                Style="{StaticResource RSApp.Styles.WarningButton}"
                                                Visibility="Collapsed" />
                                            <Button
                                                x:Name="PART_BtnCancel"
                                                Content="Cancel"
                                                Style="{StaticResource RSApp.Styles.DefaultButton}"
                                                Visibility="Collapsed" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>

                        </Border>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="rs:MessageBoxAttached.MessageBoxButton" Value="OK">
                            <Setter TargetName="PART_BtnOK" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="rs:MessageBoxAttached.MessageBoxButton" Value="OKCancel">
                            <Setter TargetName="PART_BtnOK" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_BtnCancel" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxButton" Value="YesNo">
                            <Setter TargetName="PART_BtnYes" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_BtnNo" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="rs:MessageBoxAttached.MessageBoxButton" Value="YesNoCancel">
                            <Setter TargetName="PART_BtnYes" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_BtnNo" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_BtnCancel" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="None">
                            <Setter TargetName="PART_Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Error">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#D63939" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Error}" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Hand">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#D63939" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Error}" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Stop">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#D63939" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Error}" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Exclamation">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#F76707" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Warning}" />
                        </Trigger>
                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Warning">
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Warning}" />
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#F76707" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Information">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#4299e1" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Information}" />
                        </Trigger>
                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Asterisk">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#4299e1" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Information}" />
                        </Trigger>

                        <Trigger Property="rs:MessageBoxAttached.MessageBoxImage" Value="Question">
                            <Setter TargetName="PART_Icon" Property="Fill" Value="#4299e1" />
                            <Setter TargetName="PART_Icon" Property="Data" Value="{StaticResource RSApp.Icons.Question}" />
                        </Trigger>



                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>