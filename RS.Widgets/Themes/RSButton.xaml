<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:model="clr-namespace:RS.Widgets.Models"
    xmlns:rs="clr-namespace:RS.Widgets.Controls">

    <ControlTemplate x:Key="RSApp.Styles.RSButton.Standard" TargetType="{x:Type rs:RSButton}">
        <Border
            Padding="{TemplateBinding Padding}"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{TemplateBinding CornerRadius}">
            <Grid>
                <ContentControl
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Content="{TemplateBinding Content}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>

            <Trigger Property="BtnColorType" Value="Azure">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Azure" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure.Darker}" />
            </MultiTrigger>

            <Trigger Property="BtnColorType" Value="Blue">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Blue" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Cyan">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Cyan" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Danger">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Danger" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Dark">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Dark" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Green">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Green" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Indigo">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Indigo" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Info">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Info" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Light">
                <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.LightForeground}" />
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Light" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Lime">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Lime" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Orange">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Orange" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Pink">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Pink" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink.Darker}" />
            </MultiTrigger>



            <Trigger Property="BtnColorType" Value="Primary">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary}" />
                <Setter Property="Foreground" Value="#fff" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Primary" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary.Darker}" />
            </MultiTrigger>



            <Trigger Property="BtnColorType" Value="Purple">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Purple" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple.Darker}" />
            </MultiTrigger>

            <Trigger Property="BtnColorType" Value="Red">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Red" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Secondary">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Secondary" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Success">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Success" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Teal">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Teal" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Warning">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Warning" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning.Darker}" />
            </MultiTrigger>


            <Trigger Property="BtnColorType" Value="Yellow">
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="BtnColorType" Value="Yellow" />
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow.Darker}" />
            </MultiTrigger>

        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type rs:RSButton}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Margin" Value="3,3,3,3" />
        <Setter Property="Padding" Value="2,3,2,3" />
        <Setter Property="MinWidth" Value="70" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Foreground}" />
        <Style.Triggers>
            <Trigger Property="BtnType" Value="Standard">
                <Setter Property="Template" Value="{StaticResource RSApp.Styles.RSButton.Standard}" />
            </Trigger>

            <Trigger Property="BtnType" Value="Outline">
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="#28656D77" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type rs:RSButton}">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Grid>
                                    <ContentControl
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Content="{TemplateBinding Content}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="BtnColorType" Value="Azure">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Azure" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Blue">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Blue" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Cyan">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Cyan" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Danger">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Danger}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Danger" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Dark">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Dark" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Green">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Green}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Green" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Indigo">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Indigo" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Info">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Info}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Info" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Light">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.LightForeground}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Light" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Lime">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Lime" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Orange">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Orange" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Pink">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Pink}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Pink" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Primary">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Primary}" />
                                    <Setter Property="Foreground" Value="#fff" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Primary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Purple">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Purple}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Purple" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Red">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Red}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Red" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Secondary">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Secondary}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Secondary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Success">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Success}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Success" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Teal">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Teal}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Teal" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Warning">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Warning}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Warning" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Yellow">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Yellow}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Yellow" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Foreground}" />
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="BtnType" Value="Ghost">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type rs:RSButton}">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Grid>
                                    <ContentControl
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Content="{TemplateBinding Content}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="BtnColorType" Value="Azure">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Azure" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Blue">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Blue" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Cyan">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Cyan" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Danger">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Danger}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Danger" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Dark">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Dark" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Green">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Green}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Green" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Indigo">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Indigo" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Info">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Info}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Info" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Light">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.LightForeground}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Light" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Lime">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Lime" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Orange">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Orange" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Pink">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Pink}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Pink" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Primary">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Primary}" />
                                    <Setter Property="Foreground" Value="#fff" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Primary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Purple">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Purple}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Purple" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Red">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Red}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Red" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Secondary">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Secondary}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Secondary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Success">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Success}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Success" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Teal">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Teal}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Teal" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Warning">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Warning}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Warning" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Yellow">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Yellow}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Yellow" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Foreground}" />
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="BtnType" Value="Square">
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="Template" Value="{StaticResource RSApp.Styles.RSButton.Standard}" />
            </Trigger>

            <Trigger Property="BtnType" Value="Pill">
                <Setter Property="CornerRadius" Value="15" />
                <Setter Property="Template" Value="{StaticResource RSApp.Styles.RSButton.Standard}" />
            </Trigger>

            <Trigger Property="BtnType" Value="Icon">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type rs:RSButton}">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Grid>
                                    <Path
                                        Width="{TemplateBinding IconWidth}"
                                        Height="{TemplateBinding IconHeight}"
                                        Data="{TemplateBinding IconData}"
                                        Fill="{TemplateBinding Foreground}"
                                        Stretch="Fill" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="BtnColorType" Value="Azure">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Azure" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Blue">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Blue" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Cyan">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Cyan" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Danger">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Danger" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Dark">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Dark" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Green">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Green" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Indigo">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Indigo" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Info">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Info" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Light">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.LightForeground}" />
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Light" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Lime">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Lime" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Orange">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Orange" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Pink">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Pink" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Primary">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary}" />
                                    <Setter Property="Foreground" Value="#fff" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Primary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Purple">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Purple" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Red">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Red" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Secondary">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Secondary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Success">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Success" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Teal">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Teal" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Warning">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Warning" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Yellow">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Yellow" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow.Darker}" />
                                </MultiTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>

                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="BtnType" Value="Social">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type rs:RSButton}">
                            <Border
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Path
                                        Width="{TemplateBinding IconWidth}"
                                        Height="{TemplateBinding IconHeight}"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="{TemplateBinding IconData}"
                                        Fill="{TemplateBinding Foreground}"
                                        Stretch="Fill" />
                                    <ContentControl
                                        Grid.Column="1"
                                        Margin="5,0,0,0"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        HorizontalContentAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Content="{TemplateBinding Content}" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="BtnColorType" Value="Azure">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Azure" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Azure.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Blue">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Blue" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Blue.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Cyan">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Cyan" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Cyan.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Danger">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Danger" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Danger.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Dark">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Dark" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Dark.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Green">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Green" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Green.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Indigo">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Indigo" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Indigo.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Info">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Info" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Info.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Light">
                                    <Setter Property="Foreground" Value="{DynamicResource RSApp.Styles.Button.Outline.LightForeground}" />
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Light" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Light.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Lime">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Lime" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Lime.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Orange">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Orange" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Outline.Orange.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Pink">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Pink" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Pink.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Primary">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary}" />
                                    <Setter Property="Foreground" Value="#fff" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Primary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Primary.Darker}" />
                                </MultiTrigger>



                                <Trigger Property="BtnColorType" Value="Purple">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Purple" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Purple.Darker}" />
                                </MultiTrigger>

                                <Trigger Property="BtnColorType" Value="Red">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Red" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Red.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Secondary">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Secondary" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Secondary.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Success">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Success" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Success.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Teal">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Teal" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Teal.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Warning">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Warning" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Warning.Darker}" />
                                </MultiTrigger>


                                <Trigger Property="BtnColorType" Value="Yellow">
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow}" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="BtnColorType" Value="Yellow" />
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="{DynamicResource RSApp.Styles.Button.Yellow.Darker}" />
                                </MultiTrigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

        </Style.Triggers>
    </Style>
</ResourceDictionary>